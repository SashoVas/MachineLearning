if __name__ == '__main__':
    print("""
    1. classification?: Класификация е процес на придаване(предсказване) на клас(label) от предефинирано множесто, на точка(observation).
    2. binary classification?: Класификация, но множеството от етикети(labels), е двуелементно.
    3. Which column could be the target variable for binary classification?: churn, защото изглежда, че приема като стоиности 0 и 1.
    4. What term is used to describe the other columns?: атрибути(features)
    """)
